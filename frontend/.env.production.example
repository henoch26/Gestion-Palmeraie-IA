# ================================================================
# FICHIER DE CONFIGURATION — FRONTEND REACT (production)
# ================================================================
# 1. Copier ce fichier en ".env.production" (supprimer .example)
# 2. Remplacer TON_DOMAINE.com par le vrai domaine du serveur
# 3. Ce fichier est lu UNIQUEMENT lors du build : npm run build
# 4. NE PAS committer .env.production dans Git !
# ================================================================

# URL de l'API Django — AVEC https://, SANS slash final
VITE_API_URL=https://TON_DOMAINE.com/api
